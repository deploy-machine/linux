#!/bin/sh
# OpenBSD 7.9 -- password manager
set -eu

# not applicable on OpenBSD: `flatpak install flathub com.bitwarden.desktop`
# (no flatpak, no Electron desktop client package) and bitwarden-cli is not
# packaged either (checked with pkg_info -Q). Use the web vault or the
# browser extension in firefox.
echo "passwords: not applicable on OpenBSD (no bitwarden package), nothing to do."
